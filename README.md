
  # Regulator Dashboard Design

  This is a code bundle for Regulator Dashboard Design. The original project is available at https://www.figma.com/design/NThEnorw5GSAfq621QJvdu/Regulator-Dashboard-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # front_Regulateur
